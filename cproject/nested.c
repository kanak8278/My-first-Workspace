#include <stdio.h>
int main(int argc, char const *argv[])
{
    int count;
    int sum;

    for (int i=1; i<=count; i++ )
    {
        sum=0;
        for(int j==1, j <= i ; j++)
        sum+=j;

        printf("\n %d\t %d", i, sum);
    }
    return 0;
}
